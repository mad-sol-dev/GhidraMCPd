# Xref Endpoints

## `search_xrefs_to`

Search for references pointing to a target address:

- Accepts `target`, `limit`, and `page` parameters.
- Results include caller/callee metadata plus reference kinds.
- Pagination mirrors other search endpoints with deterministic totals.
